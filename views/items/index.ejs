<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>***Item*INDEX*Project 2 Comic Index Page</title>
        <link rel="stylesheet" href="/css/app.css">
    </head>
    <body>
        <h1>Comics Index Page</h1>
        
        <nav>
            <a href="/items/new">Enter a New Comic</a>
        </nav>
        <ul>
            <% items.forEach((item, i) => { %>
            <li>
                <a href="/items/<%= item[i].id %>">
                    <%= items[i].name %>
                </a>
                			<!--  ADD DELETE FORM HERE-->
			<form action="/items/<%= item[i].id %>?_method=DELETE" method="POST">
				<input type="submit" value="Delete file"/>
            </form>
                        <!-- Add edit link here  -->
            <a href="/items/<%= item[i].id %>/edit">Edit</a>
            </li>
            <% }) %>
          </ul>
    </body>
</html>